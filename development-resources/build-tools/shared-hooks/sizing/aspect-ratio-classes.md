---
description: Generates aspect ratio classes based on props. /
---

# aspectRatioClasses

Generates aspect ratio classes based on props. /

## Signature

```javascript
aspectRatioClasses(rw)
```

## Parameters

| Parameter | Description |
|-----------|-------------|
| `rw` | - |

## Usage

```javascript
function transformHook(rw) {
    const result = aspectRatioClasses(rw);
    return { result };
}
```
