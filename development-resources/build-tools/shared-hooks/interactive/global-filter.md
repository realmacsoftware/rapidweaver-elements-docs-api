---
description: globalFilter utility function
---

# globalFilter

Utility function for interactive.

## Signature

```javascript
globalFilter(rw)
```

## Parameters

| Parameter | Description |
|-----------|-------------|
| `rw` | - |

## Usage

```javascript
const transformHook = (rw) => {
    const filter = globalFilter(rw);
    // filter.wantsFilter, filter.args
};
```
