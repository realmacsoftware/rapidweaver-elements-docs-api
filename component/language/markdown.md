# @markdown

### Editable Markdown

Add editable Markdown text areas.

```
@markdown(name: "content", title: "Content")
```

An editable Markdown text area with a default value of "Hello World!".

```
@text("heading", default: "Hello World!")
```

